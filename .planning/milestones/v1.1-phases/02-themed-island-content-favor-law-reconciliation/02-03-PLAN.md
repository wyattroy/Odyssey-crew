---
phase: 02-themed-island-content-favor-law-reconciliation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - index.html
autonomous: true
requirements:
  - CONTENT-03
must_haves:
  truths:
    - "Playing Sirens reads as rewarded temptation / wreck (D-03): daring the song grants favor (kleos) and raises the Rocks; abiding (bound to the mast) is safe but thin; giving binds a mate and pushes the Rocks back."
    - "The Sirens Dare-favor grant is the single, intentional, flagged exception to the Abide-only favor law (D-05) — recorded explicitly in the beat data."
    - "Bad Dare faces carry a wreck risk (Rocks rise faster / crew cost); the existing reef-wreck onDepart still fires when the Rocks reach doomAt."
  artifacts:
    - "EPISODES.sirens.scenes[0..2].beats — dare/abide/give × {1,3,4,6}."
    - "An explicit in-data marker/comment flagging Sirens Dare -> favor as the one sanctioned exception."
  key_links:
    - "Sirens Dare favor references CONFIG.sirens.listenFavor (or a CONFIG tier); the Rocks rise via CONFIG.sirens.worldPerListen."
    - "validateBeats() passes for all three Sirens scenes; the reef doom (world >= doomAt) onDepart path is preserved."
    - "The Sirens Dare cells are the ONLY island Dare cells that grant positive favor."
---

<objective>
Author the Sirens island onto beats as rewarded temptation with a real cost (D-03), and preserve — while explicitly flagging — its Dare-grants-favor path as the one sanctioned exception to the Abide-only favor law (D-05). All three Sirens scenes currently share the `sirensReskin()` closure (Dare grants favor, Abide no-ops, Give reduces the Rocks). This plan converts them to full beats, keeps favor on Dare (the flagged exception), makes Abide safe-but-thin, adds a wreck risk on bad Dare faces, and records the exception in the beat data so no future author copies favor-on-Dare by accident.

Purpose: Make the Sirens moral (glory has a crew-borne cost) legible on beats and lock the ONE intentional Dare-favor exception with an explicit flag.
Output: Three Sirens beats scenes with a documented Dare-favor exception and a wreck risk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-themed-island-content-favor-law-reconciliation/02-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Sirens scenes 1-3 to beats — Dare-favor (flagged) + wreck risk, Abide safe/thin, Give binds</name>
  <files>index.html</files>
  <read_first>
    - index.html — the three Sirens scenes (985-994), sirensReskin (1027-1033, the current Dare-favor + Rocks + bind logic), CONFIG.sirens (177, listenFavor/worldPerListen/bindReduce/doomAt), Sirens onDepart reef-wreck (996-1003), applyDeltas (282-289), the fx hatch in resolveEffect (738-748), validateBeats (1084-1115).
    - .planning/phases/02-themed-island-content-favor-law-reconciliation/02-CONTEXT.md — D-03 (Abide bound/thin, Dare hear song -> favor + wreck risk, scenes escalate distant song -> full lure -> the rocks), D-05 (Sirens KEEP as the flagged Dare-favor exception), D-06/D-07/D-08/D-09, and Claude's Discretion (which faces trigger wreck vs. reward).
    - .planning/research/FEATURES.md — the Sirens section (moral, the favor-path conflict, pure-abide property).
  </read_first>
  <action>
    Convert all three Sirens scenes from the shared sirensReskin closure to full beats (dare/abide/give × {1,3,4,6}). Dare = unstop your ears and listen: grant favor scaled by face (referencing CONFIG.sirens.listenFavor or a CONFIG tier) AND raise the Rocks (world) via CONFIG.sirens.worldPerListen — this favor-on-Dare is the intentional exception; on the worst face(s) add a wreck risk (Rocks rise faster and/or a crew cost) per D-03 and executor discretion. Abide = bound to the mast: safe but thin (little or no gain, no wreck) — the disciplined-but-unrewarded path here, since Sirens deliberately inverts the usual Abide-favor rule. Give = bind/stopper a mate: push the Rocks back (world down) at a small satchel cost, no favor. Escalate across scenes (distant song -> full lure -> the reef, D-09) in terse voice (D-08). Mark the Dare-favor exception explicitly in the beat data — a clearly-worded comment at the Sirens Dare block (e.g. naming it the single sanctioned Dare-favor exception per D-05) so the Plan 05 audit and future authors can see it is deliberate. Leave the reef-wreck onDepart (world >= doomAt) unchanged.
  </action>
  <verify>
    <automated>node scratchpad/harness.mjs --seed demo && node scratchpad/harness.mjs --seed alpha  # both exit 0, validateBeats ok, both reach THE VERDICT</automated>
  </verify>
  <acceptance_criteria>
    - EPISODES.sirens.scenes[0..2] each have a `beats` object with dare, abide, give all defining faces {1,3,4,6}; no scene retains a `reskin`/sirensReskin reference.
    - Sirens Dare cells grant positive favor scaled by face and raise the world (Rocks); a comment at the Dare block explicitly flags this as the one sanctioned Dare-favor exception per D-05.
    - At least one bad Dare face carries a wreck cost (extra Rocks and/or crew loss) — a 0-human transcript shows the wreck path can fire.
    - Sirens Abide cells grant no wreck and no favor scaled reward (safe/thin); Give reduces the Rocks and grants no favor.
    - validateBeats() returns { ok: true } with no Sirens scene in notYetConverted; the reef doom (world >= doomAt) still resolves in onDepart on a high-Rocks transcript.
    - `grep -nE "(you|crew|favor|world):[[:space:]]*-?[0-9]" index.html` still matches only the LAND_TABLE/SEA_TABLE fallback lines.
  </acceptance_criteria>
  <done>Sirens resolves through beats across three scenes, Dare pays favor with a flagged exception and a real wreck cost, Abide is safe/thin, Give binds, and two seeds run to THE VERDICT.</done>
  <reversibility rating="reversible">The Dare-favor exception is a flagged, documented design decision implemented as data; it can be retuned or moved to Abide later without engine change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — runtime) | Offline single-file game; integrity surface is ?seed= determinism, beats coverage, and the favor-law economy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Integrity | Sirens Dare-favor exception | medium | mitigate | The one favor-on-Dare grant is deliberate and flagged in-data; Plan 05's favor-law audit confirms it is the only island Dare-favor and that no other island copied it. |
| T-02-01 | Tampering | Sirens tell/fx closures | high | mitigate | Author is data-only; grep confirms no rnd/pick/Math.random in tell/fx; two seeds terminate. |
| T-02-02 | Tampering | beats face-key coverage | medium | mitigate | validateBeats() fail-loud gate runs each harness run. |
| T-02-SC | Tampering | package installs | n/a | accept | No package-manager installs this phase — not applicable. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- **EPISODES.sirens.scenes[0..2].beats**: dare/abide/give × {1,3,4,6} (sirensReskin no longer used by these scenes).
- **Flagged Dare-favor exception marker**: an explicit in-data comment naming Sirens Dare -> favor as the single sanctioned exception (D-05).
- Any new CONFIG.sirens scene-specific payoff magnitudes / wreck cost.
</artifacts>

<verification>
- validateBeats() ok, no Sirens scene in notYetConverted.
- Sirens Dare grants favor + raises Rocks; wreck path fires on a bad face; Abide safe/thin; Give reduces Rocks.
- The Dare-favor exception is explicitly flagged in-data.
- Two seeds reach THE VERDICT.
</verification>

<success_criteria>
Sirens reads as rewarded temptation with a crew-borne cost on beats (D-03), its Dare-favor grant is preserved as the one explicitly flagged exception (D-05), every payoff traces to CONFIG (D-06), and the seeded 0-human run completes (D-07).
</success_criteria>

<output>
Create `.planning/phases/02-themed-island-content-favor-law-reconciliation/02-03-SUMMARY.md` when done.
</output>
