---
phase: 03-anchor-verb-retrofit-balance-retune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, scratchpad/sweep.mjs, scratchpad/parity.mjs, scratchpad/econcheck.mjs]
autonomous: false
requirements: [ECON-01, ECON-02]
must_haves:
  truths:
    - "The game offers exactly two verbs at every human decision point and in every bot decision: Abide (serve the commons — always fills the shared hold, and a high roll also earns favor) and Dare (transgress for yourself — high roll fills your own stash unseen, low roll means the gods catch you and favor is lost). The third verb is gone from the engine, from the prompt, from the generic land/sea tables and from the board action bar (D-01, D-02, D-03, ECON-01)."
    - "Favor is the only divine currency: the two peril tracks (world-anger and Poseidon's curse) no longer exist as state, and no effect anywhere can move anything other than the actor's stash, the shared hold, or favor — an out-of-vocabulary payoff key fails loud instead of silently no-op'ing (D-04, ECON-02, Pitfall 10)."
    - "Aggregate living-crew favor visibly drives the world: a low-favor crew gets extra blue marbles salted into every crossing bag (longer, hungrier legs) and trips an episode's collective doom; a high-favor crew gets calm seas and the blessing branch instead. The seas/doom readout is on the board (D-05)."
    - "Helios runs entirely on the new two-verb economy across all three scenes, with payoffs escalating scene to scene (D-10), every number sourced from CONFIG (no bare integers in any beats cell), and full 1/3/4/6 face coverage per verb enforced by validateBeats."
    - "The 0-human seeded run still completes unattended to a winner across a seed sweep with zero incomplete runs, and the same ?seed= still reproduces an identical transcript."
  artifacts:
    - "index.html — new top-level `VERBS` and `DELTA_KEYS` constants; new `CONFIG.econ` (the single two-verb payoff surface) and `CONFIG.divine` (favor→seas/doom thresholds); new `econD()` beats-cell builder and `crewFavor()`/`seasExtraBlue()`/`doomFloor()`/`blessFloor()` helpers; `applyDeltas`/`validateBeats` hardened with the currency + verb whitelist; `LAND_TABLE`/`SEA_TABLE`/`askHuman('act')`/`botAct`/`verbWord`/`deltaStakes`/`renderStrip` reduced to two verbs and one currency; Helios re-authored; the other three islands mechanically converted (interim)."
    - "scratchpad/econcheck.mjs — NEW structural economy gate: comment-stripped source scan for retired identifiers plus runtime assertions on VERBS/DELTA_KEYS/newState()/every beats table. Exit 0 = the two-verb, one-currency contract holds."
    - "scratchpad/parity.mjs — NEW determinism gate: runs one seed twice in fresh contexts and compares the full transcript. Exit 0 = ?seed= still reproduces identically."
    - "scratchpad/sweep.mjs — upgraded: retired-field references dropped, plus no-winner / mean-survivors / death-count metrics and an --assert mode carrying the D-09 balance targets (used as the acceptance gate in 03-07)."
  key_links:
    - "CONFIG.econ → econD(verb, tier, face) → every beats cell `d`: one tuning surface feeds every authored payoff, so the 03-07 balance pass edits CONFIG only (BALANCE-01, EFFECT-04)."
    - "applyDeltas + validateBeats → DELTA_KEYS: the fail-loud whitelist is what makes the currency fold safe — a stale peril-track payoff key throws instead of silently resolving to nothing."
    - "crewFavor() → seasExtraBlue() → startCrossing() bag composition, and crewFavor() → doomFloor()/blessFloor() → each episode's onDepart: the single causal chain that turns private transgressions into collective weather and collective doom (D-05)."
    - "askHuman('act') → promptButtons → the per-seat ctrl-<id> board slot: dropping the third button is what makes the board action bar two-verb, and stakesLine() follows automatically from the beats tables (D-11)."
---

<objective>
Lead Phase 3 with the tracer: land the whole new economy — two verbs, one divine currency, favor-driven seas and doom — as a single vertical slice through CONFIG → engine → resolver → content → board, proven end-to-end on **one** island (Helios).

Purpose: this redesign re-opens the engine (Phase 1), all island content (Phase 2) and the board (Phase 4) at once. If the two-verb + one-currency architecture is wrong, we must find out after one commit, not after re-authoring twelve island scenes and three anchors. Every later plan in this phase (revival, the remaining islands, the three anchors, the balance tune) expands out from the skeleton proven here.

Output: a playable voyage on the new economy — two buttons on the board, favor as the only divine axis, Helios fully re-authored — plus the three measurement instruments (structural econ gate, determinism parity gate, upgraded balance sweep) that every later plan in this phase verifies against.
</objective>

<artifacts_this_phase_produces>
New in `index.html`:
- **New constants:** `VERBS = ['dare','abide']` and `DELTA_KEYS = ['you','crew','favor']` — the single sources of truth for the verb grammar and the currency vocabulary.
- **New CONFIG blocks:** `CONFIG.econ` (per-scene-tier face maps: `abideHold`, `abideFavor`, `dareStash`, `dareCaught`) and `CONFIG.divine` (`calmPerMate`, `roughStep`, `maxExtraBlue`, `doomFloorPerMate`, `blessFloorPerMate`, `doomMaxToll`).
- **New functions:** `econD(verb, tier, face, extra)` (builds a beats-cell delta from `CONFIG.econ`, so no cell ever hand-writes a number), `crewFavor()`, `seasExtraBlue()`, `doomFloor()`, `blessFloor()`, `doomToll(reason)`.
- **Removed state fields:** the two peril tracks on `state` (world-anger, Poseidon's curse) — favor absorbs both.
- **Removed verb:** the third verb — from `LAND_TABLE`/`SEA_TABLE`, `askHuman('act')`, `botAct`/`botDecide`, `verbWord`, every island's beats table, and the `button.give` CSS rule.
- **Hardened:** `applyDeltas` (currency whitelist, throws on an unknown payoff key), `validateBeats` (asserts every scene declares exactly the two verbs and every cell's `d`/`alwaysD` keys are in the currency vocabulary).
- **Re-authored:** all three Helios scenes on the two-verb economy with an escalating arc (D-10).
- **Interim (thematic authoring lands in 03-03/03-04):** Cyclops, Sirens and Lotus mechanically converted to two verbs + one currency so nothing silently no-ops.

New / changed in `scratchpad/` (dev-only, never shipped):
- **NEW `econcheck.mjs`** — the structural economy gate.
- **NEW `parity.mjs`** — the same-seed determinism gate.
- **Upgraded `sweep.mjs`** — new metrics + `--assert` mode carrying the D-09 targets.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-anchor-verb-retrofit-balance-retune/03-CONTEXT.md
@.planning/research/PITFALLS.md
@index.html
@scratchpad/harness.mjs
@scratchpad/sweep.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the three measurement instruments the whole phase verifies against</name>
  <files>scratchpad/econcheck.mjs, scratchpad/parity.mjs, scratchpad/sweep.mjs</files>
  <read_first>
    scratchpad/harness.mjs — the DOM-stub + `vm` context loader (`makeElement`/`documentStub`/`loadScriptBody`) and the two-script pattern (run index.html's inline script, then a second script in the SAME context that references `state`/`newState`/`runGame`/`validateBeats` directly). scratchpad/sweep.mjs — the per-seed runner and the report block it prints. index.html — `newState()` (the state shape), `validateBeats()`, `EPISODES`, `state.log` entry shape `{html, cls}`.
  </read_first>
  <action>
    Create the verification scaffolding this phase's `<verify>` blocks depend on (Nyquist: these tools do not exist yet, so they are built before the tracer they gate).

    (a) NEW `scratchpad/econcheck.mjs` — the structural economy gate, reusing harness.mjs's DOM-stub + vm loader verbatim. It performs TWO kinds of check and exits 1 with a per-failure diagnostic on any of them, 0 otherwise:
      1. **Source scan.** Read index.html's inline script body, strip line comments, block comments and string/template literals, then assert ZERO surviving occurrences of the retired identifiers `state.world`, `state.curse`, `.world`, `d.world`, `worldToExtraBlue`, `boastCurse`, and the retired verb key `give` (as an object key `give:` / a quoted `'give'` / a property access `.give`). Comment/prose stripping is what makes this gate robust — a design note that mentions a retired name must never fail the build, only live code may.
      2. **Runtime assertions** in a fresh vm context: `VERBS` is exactly `['dare','abide']`; `DELTA_KEYS` is exactly `['you','crew','favor']`; a `newState({...})` object has no own `world` and no own `curse` property; `LAND_TABLE` and `SEA_TABLE` each declare exactly the two verbs; every scene of every entry in `EPISODES` (and, once it exists, `ANCHORS` — probe with `typeof ANCHORS !== 'undefined'` so this tool works from this plan onward) declares exactly the two verbs with all four faces `{1,3,4,6}`, and every cell's `d` and `alwaysD` keys are inside `DELTA_KEYS`; `validateBeats()` returns `{ok:true}`.
      Print a one-line PASS summary naming how many scenes and cells were checked.

    (b) NEW `scratchpad/parity.mjs` — the determinism gate. Usage `node scratchpad/parity.mjs [--seed <name>]` (default a fixed seed). Run the SAME seed twice, each in a freshly created vm context (fresh `registry`, fresh sandbox), each with the identical 0-human options harness.mjs uses. Join each run's `state.log` entries into one string and compare. Exit 0 on an exact match printing the log length and the final verdict line; exit 1 on mismatch printing the first differing entry index and both sides. This is the mechanical proof that `?seed=` reproducibility survives every plan in this phase.

    (c) UPGRADE `scratchpad/sweep.mjs`. Drop the two retired peril-track fields from the per-seed return object and from the report. Add to the returned per-seed record: `over` (did the run reach finishGame), `deaths` (count of `l-die` class entries in `state.log`), `winnerNamed` (whether a THE VERDICT winner line was emitted). Add to the report: `no-winner / incomplete` count, mean survivors per seed, the share of seeds in which at least one death occurred, and keep the existing all-dead %, survivor distribution, winner-favor min/avg/max + distinct spread, and per-temperament alive-rate + avg favor. Then add an `--assert` mode (`node scratchpad/sweep.mjs <N> --assert`) that evaluates the run against a clearly-labelled `TARGETS` object at the top of the file and exits 1 listing every unmet target, 0 when all pass. Seed `TARGETS` with the D-09 acceptance bar (these are the gate 03-07 must clear, NOT this plan): `noWinner === 0`, `errors === 0`, `allDeadPct <= 10`, `meanSurvivors >= 2.5`, `seedsWithADeathPct >= 20` (real pressure survives — not trivial survival), `distinctWinnerFavors >= 5` (favor stays contested), `greedyAliveRate >= piousAliveRate` (defection survives), and `piousAvgFavor > greedyAvgFavor` (cooperation wins). Keep the default (no-flag) invocation reporting-only so earlier plans can read the numbers without gating on them.

    All three tools stay dev-only scratchpad files — they never become a dependency of index.html and never inject anything into it. Per ECON-01, ECON-02, BALANCE-02, D-09.
  </action>
  <verify>
    <automated>node scratchpad/parity.mjs --seed demo &amp;&amp; node scratchpad/sweep.mjs 20</automated>
  </verify>
  <acceptance_criteria>
    - `node scratchpad/parity.mjs --seed demo` exits 0 against the CURRENT (pre-tracer) index.html — proving the parity tool itself is sound before it is used as a gate.
    - `node scratchpad/sweep.mjs 20` exits 0 and prints the upgraded report including the no-winner/incomplete count, mean survivors and the death-occurrence share.
    - `node scratchpad/sweep.mjs 20 --assert` runs and exits NON-zero against the current index.html, listing the unmet D-09 targets — proving the assert mode actually discriminates (the documented pre-redesign baseline is ~50% all-dead, max 1 survivor).
    - `node scratchpad/econcheck.mjs` runs to completion and exits NON-zero against the current index.html, naming the retired identifiers and the third verb it still finds — proving the structural gate discriminates before the tracer flips it green.
    - All three tools live under `scratchpad/`; `index.html` is unmodified by this task (`git diff --stat index.html` shows no change).
  </acceptance_criteria>
  <done>parity.mjs and econcheck.mjs exist and run; sweep.mjs reports the new metrics and its --assert mode fails against the pre-redesign build with the D-09 targets listed; index.html untouched.</done>
  <reversibility rating="reversible">Dev-only scratchpad tooling; deleting the files restores the prior verification setup exactly.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: DECISION GATE (one-way) — fold the three-verb grammar into two verbs</name>
  <decision>Remove the third verb (Give) from Odyssey Crew permanently, leaving Abide / Dare as the whole verb grammar (D-01, ECON-01).</decision>
  <context>
    This is rated **one-way** in 03-CONTEXT.md. It re-authors every island's beats table, changes the engine's verb enum, the bot policy, the generic land/sea tables, the board's action bar and the project's stated "three-verb grammar" identity in PROJECT.md. Undoing it after the next six plans land is a full revert of the phase, not a patch. Confirming here is the last cheap moment.

    What replaces it: Give's function (put a ration into the commons) becomes Abide's *base* effect — Abide always fills the shared hold, and a high roll additionally earns favor (D-02). Dare keeps the self-interested road: a high roll fills your own stash unseen, a low roll means the gods catch you and favor is lost (D-03). The commons is still served; it is served by the same verb the gods reward, which is the point of the xenia frame.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the two-verb fold</name>
      <pros>Delivers ECON-01 and the whole phase goal; removes the verb that was riskless, favor-neutral and (per the measured baseline) rarely the deciding choice; makes every scene a real dilemma between the commons and the self.</pros>
      <cons>One-way; invalidates Phase 2's twelve authored Give tables and PROJECT.md's stated identity; every later plan in this phase depends on it.</cons>
    </option>
    <option id="halt">
      <name>Halt and keep three verbs</name>
      <pros>Preserves Phase 2's authored content untouched.</pros>
      <cons>Abandons ECON-01 and the phase goal; the measured death-spiral baseline (~50% all-dead) stands.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, or halt (and say what to reconsider).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: DECISION GATE (one-way) — delete the two peril tracks, favor becomes the only divine currency</name>
  <decision>Delete the world-anger and Poseidon's-curse tracks from game state and re-express both through favor (D-04, ECON-02).</decision>
  <context>
    Also rated **one-way** in 03-CONTEXT.md: it removes tracked state, rewrites every payoff site that referenced those tracks, and rewires the crossing-bag salting plus both collective-doom triggers (Helios, Sirens) onto aggregate crew favor. Every island's beats numbers get re-derived against the new signal, so a later reversal means re-authoring, not reverting a flag.

    What replaces it: aggregate living-crew favor is the world's mood. Below a per-mate calm line the marble bag salts toward blue (longer, hungrier legs); at or below the doom floor an episode's catastrophe fires; at or above the bless floor the blessing branch fires instead. Private transgressions each cost favor, and their sum is the collective doom — the commons tragedy made literal, and deterministic (favor is deterministic state).

    Safety note baked into this task: doom thresholds scale **per living crew member**, so losing crew never by itself pushes the crew toward doom — the anti-spiral guard that keeps the redesign from re-creating the failure it exists to fix.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the currency fold</name>
      <pros>Delivers ECON-02; collapses three divine-ish signals into one legible axis that is simultaneously the win condition, the lifeline and the world's mood; makes the board readable with one number.</pros>
      <cons>One-way; every existing peril-track payoff must be re-derived; two board readouts are retired.</cons>
    </option>
    <option id="proceed-guarded">
      <name>Proceed, but keep doom thresholds absolute rather than per-living-mate</name>
      <pros>Simpler arithmetic; matches the literal "aggregate favor" phrasing of D-05 with no scaling.</pros>
      <cons>Re-introduces a positive feedback loop — fewer crew means less aggregate favor means more doom means fewer crew. This is the documented death-spiral shape; not recommended.</cons>
    </option>
    <option id="halt">
      <name>Halt and keep the peril tracks</name>
      <pros>Leaves the existing (working) doom mechanics untouched.</pros>
      <cons>Abandons ECON-02 and success criterion 2; favor stays one signal among three.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, proceed-guarded, or halt (and say what to reconsider).</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>Task 4: TRACER — the whole new economy, end-to-end, proven on Helios</name>
  <files>index.html</files>
  <read_first>
    index.html, in this order: the `CONFIG` object (`fx` tiers, `helios`, `sirens`, `cyclops`, `boastCurse`, `worldToExtraBlue`, `crossing`, `log`) at ~215; `LAND_TABLE`/`SEA_TABLE` ~263; `newState()` ~301 (the two peril-track fields at ~320); `applyDeltas` ~339 and its floor comment; `deltaStakes`/`stakesLine` ~360; `askHuman('act')` ~684 (the three-button block and the stakes preview); `botDecide`/`botAct` ~731–831 (every third-verb return, and the per-episode overrides that reference the peril tracks); `canAffordDraw` ~857; `resolveEffect` ~927 (the beats/`alwaysD`/`fx` contract); `verbWord` ~1011; `EPISODES.helios` ~1024–1154 (all three scenes' beats + `onDepart` doom/bless branch); the peril-track payoff sites in `EPISODES.cyclops` (`prideSubCommit` ~1279) and `EPISODES.sirens` (every `world:`/`alwaysD` cell + `onDepart` ~1434); `validateBeats` ~1607; `startCrossing` ~1641; `renderStrip` ~515 (the two peril-track `.stat` cards); the `button.give` CSS rule ~36. Also 03-CONTEXT.md D-01/D-02/D-03/D-04/D-05/D-10/D-11 and PITFALLS.md Pitfalls 10 (silent no-op cells) and 12 (magic numbers).
  </read_first>
  <action>
    Land the new economy as ONE vertical slice: CONFIG → engine → resolver → validator → content (Helios) → board. Work in this order so the file is never in a half-vocabulary state.

    **1. Declare the vocabulary.** Add two top-level constants beside CONFIG: `VERBS = ['dare','abide']` and `DELTA_KEYS = ['you','crew','favor']`. These are the single sources of truth the validator and the structural gate read.

    **2. Add the two new CONFIG blocks.** `CONFIG.econ` holds the whole payoff surface as per-scene-tier face maps — `abideHold[tier][face]` (rations into the shared hold), `abideFavor[tier][face]` (the gods' notice on a high roll), `dareStash[tier][face]` (rations into the actor's own satchel when unseen), `dareCaught[tier][face]` (favor LOST when a low roll means the gods catch you; negative values). `tier` is 0/1/2 = an episode's first/second/third scene, and magnitudes must be non-decreasing tier to tier on both roads — that IS the escalating arc (D-10), and it settles the Phase-2 WR-02 leftover (flat numbers under rising narration). First-pass values (03-07 tunes them, this plan only has to make the shape right): `abideHold` [{1:1,3:1,4:2,6:2},{1:1,3:2,4:2,6:3},{1:2,3:2,4:3,6:3}], `abideFavor` [{6:1},{4:1,6:1},{4:1,6:2}], `dareStash` [{3:1,4:2,6:3},{3:1,4:3,6:4},{3:2,4:4,6:6}], `dareCaught` [{1:-1},{1:-2,3:-1},{1:-3,3:-1}]. `CONFIG.divine` holds the favor→world thresholds: `calmPerMate`, `roughStep`, `maxExtraBlue`, `doomFloorPerMate`, `blessFloorPerMate`, `doomMaxToll`.

    **3. Add the cell builder and the divine helpers.** `econD(verb, tier, face, extra)` returns a plain delta object assembled from `CONFIG.econ` (Abide → `{crew, favor?}`, Dare → `{you?, favor?}`), shallow-merged with an optional `extra` whose values must themselves be CONFIG-sourced — this is how a scene adds its own thematic kicker (e.g. a Dare-6 bounty that also swells the hold) without ever writing a bare integer (EFFECT-04, Pitfall 12). Omit zero-valued keys so the stakes preview stays clean. Then: `crewFavor()` = the summed favor of living crew; `seasExtraBlue()` = extra blue marbles, clamped to `maxExtraBlue`, computed from how far `crewFavor()` sits below `CONFIG.divine.calmPerMate * livingCount()` in `roughStep` increments; `doomFloor()` = `CONFIG.divine.doomFloorPerMate * livingCount()`; `blessFloor()` = `CONFIG.divine.blessFloorPerMate * livingCount()`. Scaling both thresholds per living mate is the anti-spiral guard confirmed at the decision gate — a shrinking crew must never by itself drive the crew toward catastrophe. None of these helpers may call the RNG.

    **4. Fold the currency.** Delete the two peril-track fields from `newState()`. In `applyDeltas`, iterate the delta's own keys first and `throw` a named error for any key outside `DELTA_KEYS`, then apply stash / hold / favor exactly as today (keep the existing zero floors on stash and hold; favor may go negative only if 03-02 later decides so — for now keep favor unclamped as it is today). Drop the retired peril-track branch entirely. In `deltaStakes`, drop the retired peril-track column so the preview vocabulary matches the currency vocabulary. Delete `CONFIG.worldToExtraBlue` and `CONFIG.boastCurse`; in `startCrossing`, compose the bag from `CONFIG.crossing` plus `seasExtraBlue()` and log the salting line in terms of the crew's standing with the gods rather than a curse. In `renderStrip`, replace the two retired `.stat` cards with ONE always-visible card: the crew's favor with the gods (`🫒 crewFavor()`), the seas it produces (calm, or rough with the extra-blue count), and the doom line it is measured against — this is what makes success criterion 2's "visibly roughens" literally visible on the board.

    **5. Fold the verb.** Rebuild `LAND_TABLE` and `SEA_TABLE` as two-verb tables built from `econD(verb, 0, face)` (sea keeps its distinct flavor through `verbWord`, not through a different payoff shape). In `askHuman`'s act branch, drop the third button, its label fallback, its affordability guard and its stakes line, leaving Dare and Abide with their `stakesLine()` previews (the preview follows the beats tables automatically — D-11). Update the act hint so it no longer promises three boxes. While you are in the prompt block, correct the eat hint's stale line about lots being cast — hold shortfall has been decided by fixed turn order since RESOLVE-02. In `botDecide`/`botAct`, remove every return of the third verb and every reference to the retired peril tracks, and re-express the policy on the new economy: a hold-short crew should ABIDE (Abide is now what fills the commons — this is the cooperation incentive the whole redesign turns on), a hungry or greedy sailor should DARE, `protectsHold` maps to Abide, and the per-episode overrides keep their existing character within the two verbs. In `verbWord`, keep the land/sea wording for both verbs and drop the retired third word. Delete the `button.give` CSS rule.

    **6. Harden the validator.** In `validateBeats`, additionally assert (fail-loud, same throw path as today): every scene's beats declares exactly the verbs in `VERBS` and nothing else; every declared verb still covers all four faces `{1,3,4,6}`; and every cell's `d` and `alwaysD` use only keys inside `DELTA_KEYS`. Together with `applyDeltas`'s throw, this is the mechanical guarantee that no stale payoff key can survive as a silent no-op (Pitfall 10).

    **7. Re-author Helios (the island this tracer proves the model on).** All three scenes, two verbs, four faces, every `d` built through `econD` at tier 0/1/2. Abide is the restraint-and-commons road: it forages for the ship and the gods notice on a high roll — it is never "bad", its cost is that it never fills your own belly. Dare escalates the cattle transgression: high faces are a clean kill unseen (stash, and at the climactic face a bounty that also swells the hold via `econD`'s `extra`), low faces are the Sun seeing you — favor lost. Rewrite every `tell` sentence to match the new payoff (no `tell` may describe an effect the cell no longer has). Rewrite `helios.onDepart` to read `crewFavor()` against `doomFloor()`/`blessFloor()` instead of the retired track, keeping the same three branches (catastrophe / blessing / simmering escape) and keeping `CONFIG.helios.restraintBless` as the blessing magnitude. The catastrophe branch must NOT drown the whole living crew: cap it at `CONFIG.divine.doomMaxToll` and never take the last living sailor — a wipe of a favor-bankrupt crew is exactly the dead-end this phase exists to remove (D-07, ANCHOR-04). Retire the episode's `worldStart`/`doomAt`/`trackName` fields now that nothing reads them.

    **8. Convert the other three islands mechanically (INTERIM — thematic authoring is 03-03/03-04).** Cyclops, Sirens and Lotus must be valid under the new vocabulary immediately or the validator throws. For each: fold the third verb's table into `abide` (its hold contribution IS Abide's base effect now), carrying over any load-bearing `fx` — Cyclops's wine cells must keep incrementing the drunk counter under Abide or the stake scene breaks; re-express every retired peril-track payoff as favor (a payoff that used to anger the world becomes favor lost by the actor; one that used to appease it becomes favor gained); rewrite `cyclops.prideSubCommit` so a boast costs the boaster favor from a named `CONFIG.cyclops` key instead of feeding a curse track; rewrite `sirens.onDepart` to read `crewFavor()` against `doomFloor()` with the same never-wipe-the-crew cap as Helios. Keep each cell's existing `tell` only where it is still true; where the payoff changed, adjust the sentence minimally. Mark this block with a clear in-file comment naming it interim and naming the plan that finishes it. Do not attempt the escalating arc or the thematic re-authoring for these three here — that is deliberate scope, not an oversight.

    Determinism is non-negotiable throughout: no RNG call may move into a render, click, timer or animation callback, and no new RNG call may be added to a path that did not already have one (Pitfall 5). Per D-01, D-02, D-03, D-04, D-05, D-10, D-11, ECON-01, ECON-02.
  </action>
  <verify>
    <automated>node scratchpad/econcheck.mjs &amp;&amp; node scratchpad/harness.mjs --seed demo &amp;&amp; node scratchpad/parity.mjs --seed demo &amp;&amp; node scratchpad/sweep.mjs 40</automated>
    <browser>Orchestrator serves index.html at http://localhost:8777/?seed=demo&amp;humans=1&amp;speed=0 and screenshots a human act prompt: exactly two verb buttons with their per-face stakes previews, and the crew-favor/seas/doom card in the status strip.</browser>
  </verify>
  <acceptance_criteria>
    - `node scratchpad/econcheck.mjs` exits 0 — the source scan finds no live reference to either retired peril track or to the retired verb, `VERBS`/`DELTA_KEYS` hold exactly the intended values, a fresh `newState()` has neither retired field, both generic tables declare exactly two verbs, and every scene of every episode declares exactly two verbs with all four faces and only in-vocabulary payoff keys.
    - `node scratchpad/harness.mjs --seed demo` exits 0 (validateBeats ok, finishGame reached, a THE VERDICT line emitted), and repeats exit 0 for at least three further seeds.
    - `node scratchpad/parity.mjs --seed demo` exits 0 — the same seed still reproduces an identical transcript.
    - `node scratchpad/sweep.mjs 40` reports `errors/incomplete: 0` and `no-winner: 0` — every run still terminates at a winner. (The balance TARGETS are NOT a gate for this plan; record the numbers in the SUMMARY as the post-tracer baseline for 03-07.)
    - Every Helios beats cell's `d` is produced by `econD(...)` — no bare integer literal appears in any Helios payoff, and `CONFIG.econ` is the only place Helios's magnitudes live.
    - A `?seed=` run's transcript shows a crossing whose bag was salted by low crew favor, and a Helios departure that resolves through one of the three favor-read branches — the favor→seas→doom chain is exercised, not merely present.
    - Screenshot shows exactly two verb buttons in the acting player's board card, each with its 1/3/4/6 stakes preview, and one status card showing crew favor plus the seas/doom line.
  </acceptance_criteria>
  <done>Two verbs and one currency everywhere in the engine, generic tables, prompts, bots and board; Helios fully re-authored on CONFIG.econ with an escalating arc; the other three islands mechanically valid; econcheck, harness, parity all exit 0 and the sweep shows every run reaching a winner.</done>
  <reversibility rating="one-way">Implements D-01 and D-04 — both confirmed at the gates in Tasks 2 and 3. Removes tracked state and the third verb across the engine, all content and the board; reversal after this point is a revert of the whole phase, not a patch.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human-verify the two-verb board and the favor-driven seas</name>
  <what-built>The new economy's player-facing surface: a two-button action bar (Dare / Abide) with per-face stakes previews on the board, the retired peril-track readouts replaced by a single crew-favor card that states the seas it is producing and the doom line it is measured against, and Helios playing on the new payoffs.</what-built>
  <how-to-verify>
    Open http://localhost:8777/?seed=demo&amp;humans=1&amp;speed=550 and play a few beats. Confirm: (1) every act decision offers exactly TWO buttons, Dare and Abide, each showing what each 🎲 face grants — no third verb anywhere, and no button that is permanently disabled; (2) Abide's preview shows it filling the shared hold on every face with favor appearing on the high faces, and Dare's shows it filling your own stash on high faces with favor being LOST on low faces — the dilemma reads correctly without explanation; (3) the status strip shows one crew-favor card naming the current sea state and the doom line, and the marble bag visibly holds more blue when that favor is low; (4) playing Helios, the story sentences match what the numbers actually did; (5) the shipped dark-navy aesthetic is unchanged and the page does not force horizontal scroll.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what reads wrong (button labels, stakes preview, favor/seas card, Helios narration mismatch).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored beats data → resolver → state | A payoff cell is data that mutates the commons; a stale or out-of-vocabulary key must fail loud, never resolve to a silent nothing. |
| deterministic reducer state → board render | The board projects the new favor/seas signal; it must never become a second source of truth or introduce randomness (reproducibility boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | applyDeltas / beats cells after the currency fold | critical | mitigate | `DELTA_KEYS` whitelist enforced twice — `applyDeltas` throws on an unknown payoff key at runtime, `validateBeats` rejects one at load; `econcheck.mjs` proves both plus a comment-stripped source scan for retired identifiers (Pitfall 10). |
| T-03-02 | Tampering | startCrossing / seasExtraBlue / onDepart doom branches | high | mitigate | All three read only deterministic state (`crewFavor()`, `livingCount()`); no RNG call is added or relocated into a render/click/timer callback; `parity.mjs` proves same-seed transcript equality (Pitfall 5). |
| T-03-03 | Denial of Service | favor-driven collective doom | high | mitigate | Doom thresholds scale per living crew member and the catastrophe is capped at `CONFIG.divine.doomMaxToll`, never taking the last living sailor — removes the wipe-a-bankrupt-crew dead-end (D-07, ANCHOR-04); sweep reports `no-winner: 0`. |
| T-03-04 | Information Disclosure | two-verb stakes preview in the action bar | low | accept | The preview reads only the public beats payoff table (already public knowledge, unchanged from Phase 4); the roll itself stays hidden and no board zone reads `p.commit` or `p.lastBone` pre-reveal. |

_No package-manager installs in this phase (zero-dependency, offline single file) — no supply-chain (T-03-SC) row applies._
</threat_model>

<verification>
- Structural: `node scratchpad/econcheck.mjs` exits 0 (two verbs, one currency, no live retired identifiers, full face coverage).
- Headless completion: `node scratchpad/harness.mjs --seed <s>` exits 0 across at least four seeds.
- Determinism: `node scratchpad/parity.mjs --seed demo` exits 0.
- No dead-ends: `node scratchpad/sweep.mjs 40` reports zero errors/incomplete and zero no-winner runs; the full report is recorded in the SUMMARY as the post-tracer baseline.
- Browser: two verb buttons with stakes previews, and a crew-favor/seas/doom status card.
</verification>

<success_criteria>
- ECON-01: exactly two verbs everywhere the engine, the prompts, the generic tables, the bots and the board can express one; Abide fills the hold and earns favor on a high roll, Dare fills the stash and loses favor when caught.
- ECON-02: the two peril tracks are gone from state; favor is the only divine currency; aggregate living-crew favor drives crossing-bag salting and the collective doom, and it is visible on the board.
- D-10: Helios's payoffs escalate scene to scene through `CONFIG.econ` tiers.
- Determinism and the 0-human unattended run survive; every sweep run still terminates at a winner.
</success_criteria>

<output>
Create `.planning/phases/03-anchor-verb-retrofit-balance-retune/03-01-SUMMARY.md` when done. Record the full post-tracer `sweep.mjs 40` report verbatim — 03-07 tunes against the delta from it.
</output>
