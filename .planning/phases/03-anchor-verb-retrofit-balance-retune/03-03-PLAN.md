---
phase: 03-anchor-verb-retrofit-balance-retune
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [index.html]
autonomous: true
requirements: [BALANCE-01]
must_haves:
  truths:
    - "Cyclops reads as pride and cunning on two verbs: Abide is the guest-gift road (pour the wine, brace the stake, steady a mate) that fills the commons and wins the gods' notice on a high roll, Dare is the self-interested road (strike sober, drive the stake, slip out first) that fills your own stash and costs favor when the low roll means you are seen — with the drunk counter, the collective stake check, the individual escape and the boast all intact (D-01, D-02, D-03)."
    - "Lotus reads as forgetting on two verbs, keeping its deliberate inversion: Abide is the risky road that takes the fruit (a real free meal into the commons, but the escalating strand risk), Dare is the survival road that hauls yourself or a struck mate back to the ship — and a player still lotus-struck when the ship sails is still left behind (D-01, D-02, D-03)."
    - "Both islands' payoffs escalate scene to scene through CONFIG.econ tiers 0→1→2, so the numbers rise with the narration instead of staying flat under it (D-10)."
    - "Every payoff cell on both islands is built from CONFIG — no bare integer literal appears in any beats cell — and every declared verb covers all four faces, enforced by validateBeats and proven by econcheck."
    - "The 0-human seeded run still completes to a winner, the same ?seed= still reproduces identically, and the sweep still shows zero incomplete and zero no-winner seeds."
  artifacts:
    - "index.html — EPISODES.cyclops fully re-authored (three scenes on two verbs, wine/drunk carried on Abide, `stakeCheck`/`polyphemusHunger`/`prideSubCommit` retuned to the new economy) and EPISODES.lotus fully re-authored (three scenes on two verbs, `lotusAbideFx`/`lotusDareFx`/`lotusDareTell` retuned), all cells built through `econD()` with scene-tier escalation; the 03-01 interim-conversion comment block removed for both islands."
  key_links:
    - "CONFIG.econ tiers → econD(verb, sceneIndex, face, extra) → every Cyclops/Lotus cell: the islands hang off the single tuning surface 03-07 will move (BALANCE-01, EFFECT-04)."
    - "Cyclops Abide cells → state.ep.drunk fx → stakeCheck()'s threshold: the drunk mechanic must survive the verb fold intact or scene 2 becomes unwinnable — the load-bearing link this plan is riskiest around."
    - "Lotus Abide cells → lotusAbideFx(sceneIdx) → CONFIG.lotus.struckOn → lotus.onDepart: the strand mechanic is what makes Lotus's Abide genuinely risky; it must keep escalating taste → drowse → strand."
    - "cyclops.prideSubCommit → a favor cost on the boaster → crewFavor() → seasExtraBlue(): pride now roughens the seas through the same one currency, replacing the retired curse track (ECON-02)."
---

<objective>
Re-author Cyclops and Lotus from the tracer's mechanical conversion into real two-verb content — the moral of each island carried by the payoffs and the prose, with the arc escalating across its three scenes.

Purpose: 03-01 made these two islands *valid* under the new vocabulary; it deliberately did not make them *good*. Both carry load-bearing `fx` mechanics (the giant's drunkenness and escape progress; the lotus-struck flag) that the verb fold could quietly break, so they are re-authored together as one class of work, ahead of the anchors that will copy their patterns.

Output: two islands whose Abide/Dare dilemma reads correctly at the table, whose stakes rise scene to scene, and whose special mechanics still fire.
</objective>

<artifacts_this_phase_produces>
Changed in `index.html`:
- **`EPISODES.cyclops`** — all three scenes (`The Wine`, `The Stake`, `Under the Sheep`) re-authored on two verbs with `econD()`-built payoffs at tiers 0/1/2, verb labels rewritten per scene, every `tell` matched to what its cell actually does. `stakeCheck()` and `polyphemusHunger()` retuned to the new economy; `prideSubCommit()` finalised as a favor cost.
- **`EPISODES.lotus`** — all three scenes (`The Offering`, `Going Back`, `Cast Off`) re-authored on two verbs, keeping the deliberate Abide/Dare inversion; `lotusAbideFx()`, `lotusDareFx()` and `lotusDareTell()` retuned; `CONFIG.lotus.struckOn` escalation preserved.
- **Removed:** the interim-conversion comment block 03-01 left over both islands.
- **New/changed CONFIG keys:** whatever each island's thematic kicker needs (e.g. a Cyclops boast favor cost, a Lotus free-meal magnitude) — named keys inside the existing `CONFIG.cyclops` / `CONFIG.lotus` sub-objects, never bare integers.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-anchor-verb-retrofit-balance-retune/03-CONTEXT.md
@.planning/phases/03-anchor-verb-retrofit-balance-retune/03-01-SUMMARY.md
@.planning/research/PITFALLS.md
@index.html
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cyclops on two verbs — the guest-gift road and the cunning road, all three scenes</name>
  <files>index.html</files>
  <read_first>
    index.html — `EPISODES.cyclops` in full (~1156–1302: the three scenes' beats after 03-01's interim conversion, the `collectiveCheck` seams, `prideSubCommit`, `onDepart`), `polyphemusHunger()` and `stakeCheck()` (~1570–1597: the victim path, the pooled-1 wake, the high-face threshold reduced by drunkenness), `CONFIG.cyclops` (~234), 03-01's `econD()`/`CONFIG.econ` tiers and `crewFavor()`, `resolveEffect`'s `fx`/`alwaysD`/`canAffordDraw` contract (~927), `validateBeats` (~1607). Also 03-CONTEXT.md D-02/D-03/D-10 and REQUIREMENTS.md CONTENT-02 (the island's established moral) and PITFALLS.md Pitfall 11 (do not gate a scene's only good outcome on a 10%-weight face).
  </read_first>
  <action>
    Re-author Cyclops so the two verbs carry the island's moral — pride and cunning under a monster who breaks the guest-law — with the giant's mechanics intact.

    **The verb reading for this island.** Abide is the guest-gift road: Odysseus's wine is xenia weaponised, so pouring it is serving the crew's plan and the commons (it fills the shared effort against the giant) and the gods notice patience on a high roll. Dare is the self-interested road: strike sober, drive the stake hardest, slip out first — real personal upside, and the low roll is the moment the giant's hand finds you and the gods mark the recklessness with a favor cost. This mapping is what lets the drunk counter survive the verb fold: the wine cells belong on Abide.

    **Scene 1 (`The Wine`, tier 0).** Abide pours the wine — carry the drunk-counter `fx` on every Abide face, keep the cup as a cost against the ship's stores where it already was, and let the high faces earn the gods' notice through `econD`. Dare is the blind lunge at a wide-awake monster: low faces cost the actor (and favor when seen), the top face is the single sober strike that lands and advances escape progress via `fx`. Keep the existing `collectiveCheck` calling `polyphemusHunger()` on whether anything progressed.

    **Scene 2 (`The Stake`, tier 1).** Per-cell beats still narrate each sailor's individual heave or brace; the collective outcome stays entirely inside `stakeCheck()` via the unchanged `collectiveCheck` seam — no per-cell delta may decide whether the stake drives home. Abide braces the shaft and keeps pouring (drunk `fx` preserved, so the threshold reduction still works); Dare drives it home. Retune `stakeCheck()` onto the new economy: the pooled-low-face wake and its victim stay, the high-face threshold reduced by drunkenness stays, and the success branch may now award the gods' notice to the darers ONLY if you record it in-data as an explicit flagged exception — the default expectation is that Cyclops remains a no-favor-on-Dare island and the blinding pays escape progress alone. State which you chose in the SUMMARY.

    **Scene 3 (`Under the Sheep`, tier 2).** Dare slips out under the wool — keep faces 3 and 4 (40% weight each) as the primary escape triggers rather than the rare faces (Pitfall 11), the low face costing the attempt, the top face a clean escape with a scrap of luck. Abide clings tight and steadies a mate: the hold contribution at this tier plus the gods' notice on a high roll.

    **The boast.** Finalise `prideSubCommit()`: a boast costs the boaster favor from a named `CONFIG.cyclops` key, which — because favor is now the world's mood — roughens every remaining crossing through `seasExtraBlue()` exactly as the retired curse track used to. Rewrite the prompt and the log line so they say that plainly: the shout is glory, and it costs you standing with the gods and the crew calm water. Keep `onDepart`'s "never fully broke free" victim branch, and keep the victim's island-favor clawback.

    **Across all three scenes:** every `d` comes from `econD(verb, tier, face, extra)` with any thematic kicker sourced from `CONFIG.cyclops`; magnitudes are non-decreasing tier to tier (D-10); every `tell` describes exactly what its cell does after 03-01's currency fold — no sentence may promise an effect the cell no longer has; verb labels are rewritten per scene to the two-verb reading above. Remove 03-01's interim-conversion comment for this island. Per D-01, D-02, D-03, D-10, BALANCE-01.
  </action>
  <verify>
    <automated>node scratchpad/econcheck.mjs &amp;&amp; node scratchpad/harness.mjs --seed demo &amp;&amp; node scratchpad/parity.mjs --seed demo &amp;&amp; node scratchpad/sweep.mjs 40</automated>
  </verify>
  <acceptance_criteria>
    - `node scratchpad/econcheck.mjs` exits 0 — Cyclops declares exactly two verbs across all three scenes with all four faces and only in-vocabulary payoff keys.
    - `node scratchpad/harness.mjs --seed demo` exits 0 and repeats exit 0 for at least three further seeds; `node scratchpad/parity.mjs --seed demo` exits 0.
    - `node scratchpad/sweep.mjs 40` reports `errors/incomplete: 0` and `no-winner: 0`.
    - Across a multi-seed transcript sweep, the giant is successfully blinded in a meaningful share of the seeds that reach him — the stake's success branch is reachable, not a de facto dead outcome (Pitfall 11). Record the observed share in the SUMMARY.
    - Every Cyclops payoff cell's `d` is produced by `econD(...)`; every thematic kicker traces to a named `CONFIG.cyclops` key; no bare integer appears in any Cyclops cell.
    - A boast in a transcript is followed by the boaster's favor dropping and a subsequent crossing bag salted accordingly — pride roughens the seas through the one currency.
    - If the stake's success branch grants the gods' notice on Dare, that grant is recorded in-data as an explicit flagged exception with its rationale; otherwise Cyclops grants no favor on Dare anywhere.
  </acceptance_criteria>
  <done>All three Cyclops scenes read as pride/cunning on two verbs with escalating tiers, the drunk counter, stake check, escape progress, victim path and boast all fire correctly, every number traces to CONFIG, and all four gates exit 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lotus on two verbs — the fruit and the haul-back, all three scenes</name>
  <files>index.html</files>
  <read_first>
    index.html — `EPISODES.lotus` in full (~1444–1542: the three scenes after 03-01's interim conversion, the inversion comment block, `onDepart`'s stranding), `lotusAbideFx()`, `lotusDareFx()`, `lotusDareTell()` (~1555–1567), `CONFIG.lotus` (`rations`, `struckOn` escalation, ~231), `actingPl()` (~334: how the lotus-struck flag blocks acting), 03-01's `econD()`/`CONFIG.econ` tiers, `stakesLine()`'s per-row rendering for cells carrying an `fx` closure (~375 — Lotus is the island that triggers the row-per-face stakes preview, so its `tell` closures are read by the preview and must stay pure). Also 03-CONTEXT.md D-02/D-03/D-10 and REQUIREMENTS.md CONTENT-04.
  </read_first>
  <action>
    Re-author Lotus keeping its deliberate inversion: here the safe-looking verb is the dangerous one.

    **The verb reading for this island.** Abide is taking the fruit that is offered — accepting the shore's hospitality. It genuinely feeds (a real free meal, and per the new economy that meal goes into the commons rather than the taker's own belly, which is what makes it Abide) and the gods notice restraint on the high face — but on the scene's strand faces it takes you: lotus-struck, unable to act, and left behind if you are still struck when the ship sails. Dare is hauling yourself or a struck mate back to the ship: the self-interested survival move that keeps your own feet under you, paying stash rather than favor, and never earning the gods' notice — dragging the crew back is survival, not piety.

    **Escalation (D-10).** `CONFIG.lotus.struckOn[sceneIdx]` already escalates taste → drowse → strand; keep it and let the payoffs escalate with it through `econD` tiers 0/1/2, so the fruit's pull grows exactly as the risk of never leaving does. By `Cast Off` the only face that escapes the fruit should be the one the gods reward.

    **Mechanics to preserve exactly:** `lotusAbideFx(sceneIdx)` sets the struck flag from `CONFIG.lotus.struckOn` (single source of truth, read fresh, no bare integers); `lotusDareFx` finds a struck living mate, clears them at a small cost to the rescuer, and stashes the freed name on `state.ep` for `lotusDareTell` to narrate; the rescue must remain costless-if-you-have-nothing so an empty-satchel sailor can still save a mate; `onDepart` still strands and kills anyone still struck as the ship sails, and still logs the whole-crew-aboard line otherwise. Keep every `tell` a pure describer — `stakesLine()` calls them to build the per-face preview, so a `tell` that mutates would corrupt the board.

    **Prose.** Rewrite each cell's sentence to match what it now does; the fruit's meal now feeds the ship's stores rather than the eater's satchel, so the narration must say so (you eat your fill among them and carry an armful back — the shore is generous, that is the trap). Rewrite the three scenes' verb labels to the reading above. Remove 03-01's interim-conversion comment for this island and update the island's design-note block so it explains the inversion in two-verb terms. Per D-01, D-02, D-03, D-10, BALANCE-01.
  </action>
  <verify>
    <automated>node scratchpad/econcheck.mjs &amp;&amp; node scratchpad/harness.mjs --seed demo &amp;&amp; node scratchpad/parity.mjs --seed demo &amp;&amp; node scratchpad/sweep.mjs 40</automated>
    <browser>Orchestrator serves index.html at http://localhost:8777/?seed=demo&amp;humans=1&amp;speed=0, plays to the Lotus island, and screenshots the act prompt: Abide's per-face rows show the meal AND the strand risk in the same preview, Dare's show the haul-back.</browser>
  </verify>
  <acceptance_criteria>
    - `node scratchpad/econcheck.mjs` exits 0 — Lotus declares exactly two verbs across all three scenes with all four faces and only in-vocabulary payoff keys.
    - `node scratchpad/harness.mjs --seed demo` exits 0 and repeats exit 0 for at least three further seeds; `node scratchpad/parity.mjs --seed demo` exits 0.
    - `node scratchpad/sweep.mjs 40` reports `errors/incomplete: 0` and `no-winner: 0`.
    - Across a multi-seed sweep the strand mechanic still bites and is still escapable: at least one seed shows a sailor left behind on the shore, and at least one shows a struck sailor hauled back by a mate. Record both seeds in the SUMMARY.
    - The Abide stakes preview renders one row per face showing both the numeric payoff and the authored flavor (the strand risk is legible before committing) — the row-per-face path in `stakesLine()` still triggers for this island.
    - Every Lotus payoff cell's `d` is produced by `econD(...)`; the free-meal magnitude and the strand faces both trace to named `CONFIG.lotus` keys; no bare integer appears in any Lotus cell.
    - No `tell` closure on this island mutates state — they remain pure describers safe for the stakes preview to call.
  </acceptance_criteria>
  <done>All three Lotus scenes read as forgetting-versus-the-voyage on two verbs with escalating strand risk, the struck/rescue/strand mechanics all fire, the stakes preview shows the risk before commitment, and all four gates exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored beats data → resolver → episode-local state (`state.ep`) | Both islands mutate episode state through `fx` closures; a verb fold that drops an `fx` silently disables a scene's whole mechanic without any error. |
| beats `tell` closures → stakes preview render | `stakesLine()` calls `tell(p)` to build the preview; a `tell` with a side effect would mutate state from a render path and break determinism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | Cyclops drunk/progress `fx` after the verb fold | high | mitigate | The wine `fx` is carried onto Abide's cells explicitly; acceptance requires the blinding branch to be observably reachable across a multi-seed sweep, not merely present in the source (Pitfall 11). |
| T-03-10 | Tampering | Lotus `tell` closures invoked by `stakesLine()` | high | mitigate | Every `tell` stays a pure describer; the acceptance criteria assert no `tell` on this island mutates state, preserving the render-is-a-pure-projection contract (Pitfall 1/5). |
| T-03-11 | Repudiation | retuned payoffs with untraceable magnitudes | medium | mitigate | Every cell is built through `econD()` with kickers from named `CONFIG.cyclops`/`CONFIG.lotus` keys, so 03-07's balance pass edits CONFIG only (Pitfall 12). |
| T-03-12 | Denial of Service | Lotus strand + Cyclops victim paths compounding into a wipe | medium | mitigate | Revival is reachable from every beat as of 03-02, and the sweep gate requires zero no-winner runs across 40 seeds. |

_No package-manager installs in this phase (zero-dependency, offline single file) — no supply-chain row applies._
</threat_model>

<verification>
- Structural: `node scratchpad/econcheck.mjs` exits 0 (both islands, two verbs, four faces, in-vocabulary payoffs only).
- Headless completion: `node scratchpad/harness.mjs --seed <s>` exits 0 across at least four seeds.
- Determinism: `node scratchpad/parity.mjs --seed demo` exits 0.
- No dead-ends: `node scratchpad/sweep.mjs 40` reports zero errors/incomplete and zero no-winner runs.
- Mechanic liveness: transcripts show the giant blinded, a sailor stranded by the lotus, and a struck sailor hauled back.
- Browser: the Lotus act prompt renders the per-face rows carrying both payoff and strand flavor.
</verification>

<success_criteria>
- BALANCE-01: Cyclops's and Lotus's hand-tuned per-stage payoffs are authored on the new economy through `CONFIG.econ` + named episode keys, replacing the interim conversion and the pre-redesign constants.
- D-10: both islands' payoffs escalate across their three scenes rather than sitting flat under rising narration.
- The islands' validated mechanics (drunkenness, stake check, escape progress, boast, lotus-struck, rescue, stranding) all still fire.
- Determinism and the 0-human unattended run survive; every sweep run still terminates at a winner.
</success_criteria>

<output>
Create `.planning/phases/03-anchor-verb-retrofit-balance-retune/03-03-SUMMARY.md` when done. Record the `sweep.mjs 40` report, the observed blinding-success share, the stranding/rescue seeds, and whether the stake's success branch grants favor on Dare (flagged exception) or not.
</output>
