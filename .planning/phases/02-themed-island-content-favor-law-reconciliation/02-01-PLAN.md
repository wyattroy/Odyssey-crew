---
phase: 02-themed-island-content-favor-law-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - CONTENT-01
must_haves:
  truths:
    - "Playing Helios reads as restraint (D-01): Abide is inert except a 6 (forage dandelions, small gain); Dare escalates the cattle transgression with high upside and, on a 1, Helios's wrath (world+)."
    - "Each of the three Helios scenes forms a rising mini-story (D-09): hunger -> temptation -> doom, with payoffs escalating across scenes."
    - "A 0-human ?seed= run still reaches THE VERDICT with the enriched Helios numbers (D-07 gentler-survivable first draft)."
  artifacts:
    - "CONFIG.fx extended with larger named payoff tiers (e.g. big, huge) alongside tiny/small/penalty (D-06)."
    - "EPISODES.helios.scenes[0..2].beats — dare/abide/give each defined for all four faces {1,3,4,6} (12 cells per scene)."
  key_links:
    - "validateBeats() passes for all three Helios scenes (fail-loud coverage gate)."
    - "resolveEffect() consumes the new beats (beats override the mkHeliosDare reskin on scenes 2 & 3)."
    - "Every Helios beats d:{} value is a CONFIG.* reference — no bare integer literals (EFFECT-04)."
---

<objective>
Author the full Helios island (Cattle of Helios) to the beats engine as the phase tracer, and lay the CONFIG payoff-tier scaffolding every later island reuses. Helios scene 1 "The Meadow" already resolves through beats (Phase-1 tracer, behavior-preserving); this plan enriches it to the user's D-01 scale and converts scenes 2 "The Hunger" and 3 "The Reckoning" off their `mkHeliosDare` reskin onto full beats.

This is the end-to-end tracer slice: it proves the authoring pattern (CONFIG tier -> beats cells -> validateBeats coverage -> a 0-human seeded run that still completes) works through every layer BEFORE Cyclops/Sirens/Lotus replicate it. The moral of restraint (D-01) is the anchor voice and scale for the whole phase.

Purpose: De-risk the authoring pattern on one fully-worked island and establish the shared CONFIG tiers.
Output: Extended CONFIG.fx tiers, three fully-authored Helios beats scenes, and a reusable headless verification harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-themed-island-content-favor-law-reconciliation/02-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: CONFIG payoff tiers + Helios "The Meadow" enriched to D-01 scale, proven end-to-end</name>
  <files>index.html, scratchpad/harness.mjs</files>
  <read_first>
    - index.html — CONFIG block (lines ~162-202, esp. CONFIG.fx and CONFIG.helios), applyDeltas (282-289), resolveEffect (738-760), the existing Helios scene 1 beats (834-876), validateBeats (1084-1115), and runGame/finishGame (1356-1379, 1304-1329) to see where validateBeats runs and how a game terminates.
    - .planning/phases/02-themed-island-content-favor-law-reconciliation/02-CONTEXT.md — D-01 (Helios restraint), D-06 (two-tier CONFIG numbers), D-07 (gentler survivable), D-08 (terse voice), D-09 (rising arc), and the <specifics> verbatim Helios template.
    - .planning/research/PITFALLS.md — Pitfall 12 (magic numbers), Pitfall 10 (silent no-op cells), Pitfall 5 (RNG/determinism).
  </read_first>
  <action>
    Extend CONFIG.fx with larger named payoff tiers (e.g. big, huge) sufficient to express the D-01 Helios Dare-6 bounty (stash +2, hold +4) and cross-scene escalation; keep tiny/small/penalty. Values are executor discretion within D-07's gentler-survivable framing. If scene-specific magnitudes are needed, add them to the existing CONFIG.helios block, not inline (D-06). Then enrich the existing Helios scene 1 "The Meadow" beats so Dare-6 pays the D-01 bounty (stash +2 / hold +4) and Dare-1 is seen-by-the-Sun (world rises) per the <specifics> template — every delta value referencing a CONFIG tier, never a numeric literal. Create a reusable headless verification harness in scratchpad (a Node script that stubs document + the render* functions as no-ops, evaluates the index.html script body, builds a 0-human seeded state, calls runGame(), and asserts state.over is true, a THE VERDICT log line exists, and validateBeats() returns ok) — this harness is reused by every later plan in this phase. Do NOT introduce any rnd/pick/Math.random call into any tell or fx closure (Pitfall 5). Do NOT modify LAND_TABLE/SEA_TABLE (they remain the sanctioned bare-integer fallback for unconverted scenes).
  </action>
  <verify>
    <automated>node scratchpad/harness.mjs --seed demo  # exits 0, prints a THE VERDICT line, validateBeats ok (create the harness in this task if absent — Nyquist scaffold)</automated>
  </verify>
  <acceptance_criteria>
    - CONFIG.fx contains at least one payoff tier larger than `small` (a value able to express hold +4), and CONFIG.fx still defines tiny/small/penalty.
    - Helios scene 1 Dare-6 resolves to satchel +2 and hold +4 (the D-01 bounty), and Dare-1 raises the world track (seen by the Sun).
    - `node -e` / harness run: validateBeats() returns { ok: true } and a 0-human seed=demo game reaches finishGame() (state.over === true) with a THE VERDICT log entry.
    - `grep -nE "(you|crew|favor|world):[[:space:]]*-?[0-9]" index.html` matches ONLY the LAND_TABLE/SEA_TABLE fallback lines (~206-215); the Helios beats region shows zero bare-integer deltas.
    - `grep -nE "rnd\(|pick\(|Math\.random" index.html` shows no new call sites inside any `tell:` or `fx:` closure in EPISODES.helios.
  </acceptance_criteria>
  <done>CONFIG tiers exist, Helios "The Meadow" pays the D-01 bounty, the headless harness runs green, and a seeded 0-human run completes.</done>
  <reversibility rating="reversible">CONFIG tiers and beat cells are pure data edits, trivially retunable in Phase 3.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Author Helios scenes 2 "The Hunger" and 3 "The Reckoning" to full beats</name>
  <files>index.html</files>
  <read_first>
    - index.html — Helios scenes 2 & 3 (877-882, currently reskin: mkHeliosDare), mkHeliosDare (818-833), the completed scene 1 beats authored in Task 1 (the pattern to replicate), Helios onDepart (884-895), validateBeats (1084-1115).
    - .planning/phases/02-themed-island-content-favor-law-reconciliation/02-CONTEXT.md — D-01 (restraint asymmetry), D-08 (terse concrete callouts in the Helios voice), D-09 (rising arc: hunger -> temptation -> doom), <specifics> template.
  </read_first>
  <action>
    Replace the `reskin: { dare: mkHeliosDare() }` on Helios scenes 2 "The Hunger" and 3 "The Reckoning" with full beats tables: dare, abide, and give each defined for faces {1,3,4,6}, mirroring the scene-1 pattern from Task 1. Honor the D-01 asymmetry per cell: Dare escalates the cattle transgression (high faces = clean kill, big satchel/hold gain; face 1 = seen by the Sun -> world rises + favor penalty condemnation); Abide is inert except a 6 (a small forage gain, and the Abide-6 favor nod that is the default favor path); Give feeds the hold from the giver's own satchel and never moves favor. Escalate the stakes and payoffs across the three scenes (D-09) — later scenes carry the larger tiers. Author terse concrete callouts in the Helios voice (D-08). Every delta references a CONFIG tier (D-06); stateful needs (none expected for Helios) would use the `fx` hatch, but flat `d` deltas should suffice here. Leave onDepart (the restraint bless / wrath doom) unchanged — it already reads state.world.
  </action>
  <verify>
    <automated>node scratchpad/harness.mjs --seed demo && node scratchpad/harness.mjs --seed alpha  # both exit 0, validateBeats ok, both reach THE VERDICT</automated>
  </verify>
  <acceptance_criteria>
    - EPISODES.helios.scenes[1] and scenes[2] each have a `beats` object with dare, abide, give — every verb defining all four faces {1,3,4,6} (24 authored cells across the two scenes); neither scene retains a `reskin` field.
    - validateBeats() returns { ok: true } and lists NO Helios scene under notYetConverted.
    - Every Helios Abide favor grant is a positive favor only on face 6; no Helios Dare cell grants positive favor (a Dare-1 favor penalty / condemnation is allowed).
    - Payoff magnitudes are non-decreasing across scenes 1->2->3 for the Dare high-face bounty (rising arc, D-09).
    - Two distinct seeds (demo, alpha) each run 0-human to THE VERDICT — the enriched numbers keep the run survivable (D-07).
  </acceptance_criteria>
  <done>All three Helios scenes resolve entirely through beats, validateBeats passes, and Helios reads as an escalating story of restraint across two seeded runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — runtime) | Single self-contained index.html, offline, no network/storage/deps/untrusted input. The only integrity surface is the reproducibility contract (?seed= determinism) and effect-table coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | authored tell/fx closures in EPISODES.helios | high | mitigate | Author is data-only; grep confirms no rnd/pick/Math.random inside tell/fx; harness runs two seeds and both terminate identically-shaped (determinism preserved). |
| T-02-02 | Tampering | beats face-key coverage | medium | mitigate | validateBeats() fail-loud gate runs at the start of every harness run; a missing/typo face aborts loudly rather than silently no-op'ing (Pitfall 10). |
| T-02-03 | Information | CONFIG numeric tracing | low | mitigate | Bare-integer grep confirms every beats delta traces to CONFIG (EFFECT-04 retunability preserved). |
| T-02-SC | Tampering | package installs | n/a | accept | No package-manager installs this phase (no build/deps, single index.html) — supply-chain threat not applicable. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- **CONFIG.fx**: new larger payoff tiers (e.g. `big`, `huge`) alongside `tiny`/`small`/`penalty`.
- **CONFIG.helios**: any scene-specific payoff magnitudes needed beyond the shared fx tiers.
- **EPISODES.helios.scenes[0].beats**: enriched to the D-01 bounty (stash +2 / hold +4 on Dare-6, world+ on Dare-1).
- **EPISODES.helios.scenes[1].beats** and **scenes[2].beats**: new full dare/abide/give × {1,3,4,6} tables (reskin removed).
- **scratchpad/harness.mjs** (dev-only, not shipped): reusable headless Node harness that runs a 0-human seeded game and asserts validateBeats + termination.
</artifacts>

<verification>
- validateBeats() returns ok with no Helios scene in notYetConverted.
- A 0-human ?seed= run reaches THE VERDICT on at least two seeds.
- No bare-integer deltas in the Helios beats region; no rnd/pick in Helios tell/fx.
</verification>

<success_criteria>
Helios is fully authored to beats across all three scenes, reads as an escalating story of restraint (D-01/D-09), every payoff traces to CONFIG (D-06), and the seeded 0-human run still completes (D-07). The authoring pattern and CONFIG scaffolding are proven for the expansion plans.
</success_criteria>

<output>
Create `.planning/phases/02-themed-island-content-favor-law-reconciliation/02-01-SUMMARY.md` when done.
</output>
