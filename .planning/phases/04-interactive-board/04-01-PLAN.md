---
phase: 04-interactive-board
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: false
requirements: [BOARD-01, BOARD-06, BOARD-07]
must_haves:
  truths:
    - "The voyage track shows all 8 nodes (Troy → island → island → Hades → island → island → Phaeacia → Ithaca) with a ⛵ boat marker sitting on the current node (state.beatIndex) that advances as the voyage progresses; un-revealed islands render as a face-down chip until reached (BOARD-01)."
    - "Each crew card surfaces name, temperament, favor (🫒 N) and alive/starving/dead status (🟢/🟠/💀); the shared hold (🛢️ N) is shown once, prominently, as the commons; the acting player is highlighted (BOARD-06)."
    - "The board is a pure projection of state: renderBoard() and its sub-renderers read only from state; no rnd()/rint()/pick()/throwBone()/Math.random is introduced into any render, click, timer, or requestAnimationFrame callback; any board-only transient uses the existing _-prefix convention and is cleared by the existing clearBones()-style reset (BOARD-07)."
    - "A 0-human ?seed= run still completes unattended to a winner, and the same seed reproduces byte-identical output after the board scaffold lands (determinism preserved)."
  artifacts:
    - "index.html — renderBoard() orchestrator added and called from the existing render(); renderTrack() upgraded with the ⛵ boat marker; renderPlayers()/renderStrip() upgraded to the board crew-row + prominent-hold styling; board CSS classes added to the existing <style> block (dark-navy aesthetic, no whole-app restyle)."
  key_links:
    - "render() → renderBoard(): the board is composed at the existing post-mutation render() call sites (actPhase / eatPhase / runCrossing / runIsland / runGame already call render() after each state change) — no new animation state machine, no new call site."
    - "state.beatIndex + state.journey[] → boat position (the boat has NO parallel position field; it is derived every render)."
    - "state.hold / p.favor / p.status → crew row + hold readout (read directly from state, never cached in the DOM)."
---

<objective>
Lead Phase 4 with the tracer: a `renderBoard()` scaffold that draws the voyage track (with the ⛵ boat marker advancing across Troy → islands → Hades → Phaeacia → Ithaca) and the upgraded crew row (favor / hold / status), rendering PURELY from state — proving the pure-projection (BOARD-07) and determinism contracts end-to-end BEFORE any interactivity, marble, dice, or masking work is layered on.

Purpose: This is the thinnest path that touches every layer the phase will modify (CSS → render orchestrator → track → crew row → existing render() call sites) wired end-to-end. If the pure-projection or determinism contract is wrong, we find out after one commit — not after ten. Every later slice (crossing strip, action bar, masking, log demotion) expands out from this proven skeleton.

Output: An upgraded board surface that a screenshot shows as a boat on the journey track over a crew row with favor/hold/status, and a headless 0-human seeded run that still completes to a winner with byte-identical determinism.
</objective>

<artifacts_this_phase_produces>
New / changed in index.html (tracer slice only — later plans add renderBag/renderDice/renderNarration/action-bar/masking/log-demotion):
- **New function:** `renderBoard()` — the board orchestrator, called from the existing `render()`. Later plans hang `renderBag()` / `renderDice()` / `renderNarration()` off this same seam.
- **Upgraded functions:** `renderTrack()` (adds the ⛵ boat marker on `state.beatIndex`), `renderPlayers()` and `renderStrip()` (board crew-row: temperament + 🫒 favor + 🟢/🟠/💀 status; the shared 🛢️ hold shown once, prominently).
- **New CSS classes** in the existing `<style>` block (match the shipped dark-navy palette; do NOT restyle the whole app): a boat marker class, board-container/crew-row refinements. Reuse existing tokens (`--panel`, `--line`, `--gold`, `.beat.cur`, `.stat`, `.p`).
- **New `_`-transients:** none required for the tracer — the boat/crew derive from existing `state.beatIndex`/`state.journey`/`state.hold`/`p.favor`/`p.status`. If any board-only transient becomes necessary, it MUST be `_`-prefixed and cleared by `clearBones()` (BOARD-07).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interactive-board/04-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@index.html
@scratchpad/harness.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: renderBoard() scaffold + voyage track with advancing ⛵ boat marker (end-to-end, one path)</name>
  <files>index.html</files>
  <read_first>
    index.html — the RENDER section: `render()` (composes renderTrack/renderStrip/renderPlayers), `BEATS_META` (the 8-node icon/label array), `renderTrack()` (already renders journey nodes with `.beat.cur`/`.done` and island reveal), `clearBones()` (the per-phase `_`-transient reset); the `<style>` block tokens (`--panel`, `--line`, `--gold`, `.beat`, `.beat.cur`); the reducer render call sites (`runGame()` beat loop line ~1712, `runIsland()` ~1517/1528, `runCrossing()` ~1492/1503). 04-UI-SPEC.md §Layout item 1 (Voyage track) + §Build order 1 (Tracer) + §Behavioral contracts (Pure projection, Determinism).
  </read_first>
  <action>
    Introduce `renderBoard()` as a thin orchestrator called from the existing `render()` (do NOT add a new call site or animation loop — `render()` already runs after every reducer mutation). In `renderTrack()`, place a ⛵ boat marker on the node at `state.beatIndex` so the boat visibly sits on the current leg and advances as `state.beatIndex` increments through the 8-node journey; keep the existing `.beat.cur` accent ring on the current node and `.done` dimming on passed nodes; un-revealed island nodes keep rendering as the face-down chip (🎴 / "Island ?") until `beat.revealed`. Add board CSS to the existing `<style>` block using the shipped palette tokens only (a boat-marker class and any container refinement) — do NOT restyle the whole app. The track container must overflow-x: auto inside its card so a wide track never forces horizontal page scroll. Read boat position ONLY from `state.beatIndex`/`state.journey` — introduce NO parallel position field and NO randomness in any render path (BOARD-07/09). Per BOARD-01, BOARD-07.
  </action>
  <verify>
    <automated>node scratchpad/harness.mjs --seed demo</automated>
    <browser>Orchestrator serves index.html at http://localhost:8777/?seed=demo&auto=1&humans=0&speed=0 and screenshots the board: the 8-node track renders Troy → … → Ithaca with a ⛵ marker on the current node.</browser>
  </verify>
  <acceptance_criteria>
    - `node scratchpad/harness.mjs --seed demo` exits 0 (validateBeats ok + finishGame reached + a "THE VERDICT" line emitted) — proving the board scaffold did not break the 0-human seeded run.
    - Running the harness twice on `--seed demo` yields byte-identical output (same THE VERDICT winner + same final favor totals) — determinism preserved (BOARD-07/09).
    - Screenshot shows exactly 8 track nodes in order with the ⛵ boat marker on one node; passed nodes dimmed; the current node accented.
    - The boat position is derived from `state.beatIndex` every render (advancing a leg in the auto run moves the boat) — no board-local position variable survives a fresh render() call.
  </acceptance_criteria>
  <done>The ⛵ boat renders on the current journey node and advances Troy→Ithaca purely from state; renderBoard() composes from render(); the harness passes and same-seed output is byte-identical.</done>
  <reversibility rating="reversible">Rendering-only change in a single self-contained index.html — fully git-revertible; establishes but does not lock any external contract.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Upgrade the crew row — favor / status / temperament + prominent shared hold (BOARD-06)</name>
  <files>index.html</files>
  <read_first>
    index.html — `renderPlayers()` (player cards: name, badge, `.res` with 🍖/🫒, `statusTok(p)`, `_committing`, `_boneShow`, the `ctrl-<id>` slot), `statusTok(p)` (🟢/🟡/🌸/💀 mapping), `renderStrip()` (the shared-hold `.stat` with the `hold-danger` safe-line warning, plus crossing/world/curse stats), `tempIcon` map, `p._acting` highlight + `.p.acting` CSS. 04-UI-SPEC.md §Layout item 3 (Crew row).
  </read_first>
  <action>
    Upgrade `renderPlayers()` so each crew card clearly surfaces name, the player's temperament (its emoji/label — greedy/balanced/pious), favor as `🫒 N`, and alive/starving/dead status as `🟢`/`🟠`/`💀` (keep the existing lotus-struck `🌸` token). Keep the acting-player highlight (`p._acting` → `.p.acting`). Ensure the shared hold (`state.hold`) is shown ONCE, prominently, as the commons — reuse `renderStrip()`'s existing hold `.stat` and its `hold-danger`/safe-line treatment rather than duplicating the number onto every card. All values read directly from `state` on each render; add NO persistent fields (BOARD-07). Style within the existing dark-navy tokens; do not restyle unrelated panels. Per BOARD-06.
  </action>
  <verify>
    <automated>node scratchpad/harness.mjs --seed demo</automated>
    <browser>Screenshot the running board: the crew row shows all 4 players with temperament + 🫒 favor + status token, and the shared 🛢️ hold appears once as a prominent stat.</browser>
  </verify>
  <acceptance_criteria>
    - Each of the 4 crew cards shows favor (🫒), a status token (🟢 alive / 🟠 starving / 💀 dead), and temperament; the acting player is visibly highlighted during an act.
    - The shared hold (🛢️) is rendered exactly once (in the strip), not repeated per card; its danger/safe-line state still reflects `state.hold` vs living count.
    - `node scratchpad/harness.mjs --seed demo` still exits 0 (crew-row changes did not perturb the reducer path or determinism).
  </acceptance_criteria>
  <done>Crew row shows favor/status/temperament per player with the acting player highlighted, the shared hold shown once prominently; harness passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the tracer board (boat on track + crew row)</name>
  <what-built>The tracer board: `renderBoard()` scaffold, the voyage track with an advancing ⛵ boat marker, and the upgraded crew row (favor / status / temperament) with the shared hold shown prominently — all rendering purely from state.</what-built>
  <how-to-verify>
    Open the board served at http://localhost:8777/?seed=demo&auto=1&humans=0&speed=550 (brief pause so the boat advance is visible). Confirm: (1) the journey track shows 8 nodes Troy → islands → Hades → Phaeacia → Ithaca with a ⛵ boat that moves node-to-node as the voyage progresses and un-revealed islands shown face-down until reached; (2) the crew row shows each player's favor, alive/starving/dead status, and temperament, with the acting player highlighted; (3) the shared hold is shown once, prominently; (4) the shipped dark-navy aesthetic is preserved (the app was not restyled wholesale); (5) the page does not force horizontal scroll.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (boat placement, missing status, restyle regressions, layout overflow).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer state → board render | The board is a downstream projection; it must never become a second source of truth or add randomness (reproducibility + state-integrity boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | renderBoard()/renderTrack() and any board render path | high | mitigate | No rnd()/rint()/pick()/throwBone()/Math.random introduced in any render/click/timer/rAF callback; boat/crew derive only from already-decided state; enforced by same-seed byte-identical harness parity (BOARD-07/09). |
| T-04-02 | Information Disclosure | crew-row render | medium | mitigate | Tracer renders only public common-knowledge zones (boat position, hold, favor, alive/dead) — it does not read `p.commit` or `p.lastBone`; the private-until-reveal zones (verb pick, dice) are introduced under their own masking gates in plans 04-02/04-04. |

_No package-manager installs in this phase (zero-dependency, offline single file) — no supply-chain (T-*-SC) row applies._
</threat_model>

<verification>
- Headless: `node scratchpad/harness.mjs --seed demo` exits 0; running it twice on the same seed produces byte-identical output (determinism).
- Browser: served board screenshot shows the 8-node track with the ⛵ boat marker and the upgraded crew row; no horizontal page-scroll overflow.
- Non-regression: the full voyage still runs to a winner in the 0-human seeded auto run.
</verification>

<success_criteria>
- BOARD-01: the voyage track visibly shows the boat advancing across the 8-node Troy→Ithaca sequence, purely from state.
- BOARD-06: each player's favor/status/temperament and the shared hold are visible on the board.
- BOARD-07: renderBoard() is a pure projection — no parallel state, no new randomness in render/click callbacks, `_`-prefixed transients only (none needed for the tracer), cleared by clearBones().
- Determinism intact: same `?seed=` reproduces identical output; the 0-human seeded run completes unattended to a winner.
</success_criteria>

<output>
Create `.planning/phases/04-interactive-board/04-01-SUMMARY.md` when done.
</output>
